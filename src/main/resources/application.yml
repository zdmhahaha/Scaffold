server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vxshop?useSSL=false
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
# 日志配置
logging:
  level:
    com.example: debug
    org.springframework: warn